pygame.init()
    pantalla = pygame.display.set_mode((ancho,alto))
    # Titulo e iconos
    pygame.display.set_caption("Aje-Chess")
    icono = pygame.image.load("chess.png")
    pygame.display.set_icon(icono)
    pantalla.fill(pygame.Color())
    game = game.Juego()  #variable para nuestra funcion del game file
    reloj = pygame.time.Clock()
    juego_comenzado

    # Loop del juego para que la pantalla no se cierre al instante y cerrar el juego cuando queramos
    juego_terminado = False'


    Dinamica del juego :

    -Es un juego basado en Pokemon y ajedrez
    -
    -
    -
    -